<?php

namespace Fmt\Fixers;

interface FixerInterface
{
    /*
     * Accepts a string containing the source that
     * the fixer needs to fix.
     *
     * @param      string  $source  The source
     */
    // public function fix(string $source) : string
}
