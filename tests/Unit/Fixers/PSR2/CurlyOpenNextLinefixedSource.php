<?php

namespace Qualified\Name;

abstract class SomethingHere 
{
    // Curly braces on the same line as the class?
    // no way!
    public function doSomething($withParams = []) 
	{
        // functions with braces on the same line?
    }
}
